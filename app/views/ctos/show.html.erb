<p>
  <strong>Назва:</strong>
  <%= @cto.name %>
</p>
 
<p>
  <strong>Опис:</strong>
  <%= @cto.description %>
</p>

<p>
  <strong>Адреса:</strong>
  <%= @cto.address %>
</p>


<p>
  <strong>Контакти:</strong>
  <%= @cto.contacts %>
</p>


<p>
  <strong>Розклад роботи:</strong>
  <%= @cto.schedule %>
</p>


<h2>Послуги які пропонує сервіс</h2>

<table class="table table-striped table-bordered table-condensed">
  <thead>
  <tr>
    <th>Опис послуги</th>
    <th>Вартість</th>
    <th>Операції</th>
  </tr>
  </thead>
  <tbody>
  <% @cto.services.each do |service| %>
      <tr>
        <td><%= service.description %></td>
        <td><%= service.price %></td>
        <td><%= link_to 'Замовити послугу', new_cto_service_order_path(@cto.id, service.id) %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<h2>Додати нову послугу (тільки для користувачів рівня адміністратора):</h2>
<%= form_for([@cto, @cto.services.build]) do |f| %>
    <p>
      <%= f.label :Description %><br>
      <%= f.text_field :description %>
    </p>
    <p>
      <%= f.label :Price %><br>
      <%= f.text_field :price %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>

<h2>Відгуки про дану сто (todo: add other fields and styling)</h2>
<% @cto.services.each do |service| %>
    <hr>
    <p>Evaluated service:<%= service.description %></p>
    <% service.reviews.each do |review| %>
        <p>
          Description:<%= review.reviewText %>
        </p>

        <p>
          Mark:<%= review.mark %>
        </p>

    <% end %>
<% end %>

<%= link_to 'Редагувати дані компанії', edit_cto_path(@cto) %> |
<%= link_to 'На головну', ctos_path %>